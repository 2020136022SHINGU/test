week02
- linux commands
  ls, ls -a ls -l, ls -al
  pwd, cd .git, cd.., touch week01.txt
  vi week01.txt, vi week02.txt, 
  yy, p
  echo "sdadsddssda" >> README.md
  cat README.md

- git commands
  git init
  git status
  git add week01.txt
  git config --global user.email ""
  git config --global user.name ""
  git branch -M main
  git remote add origin https://github.com/2020136022SHINGU/test.git
  git push -u origin main
 